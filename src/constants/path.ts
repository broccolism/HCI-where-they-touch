export enum CustomPath {
  HOME = "/",
  NOTICE = "/notice",
  KEYBOARD = "/keyboard",
  QUESTION = "/question",
  RESULT = "/result",
}

export enum CookieName {
  TOUCHES = "touches",
  SIZE = "size",
  ANSWERS = "answers",
}
