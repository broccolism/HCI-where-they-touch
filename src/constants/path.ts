export enum CustomPath {
  HOME = "/",
  NOTICE = "/notice",
  KEYBOARD = "/keyboard",
  CHECKBOX = "/check",
  RESULT = "/result",
}

export enum CookieName {
  TOUCHES = "touches",
  SIZE = "size",
}
